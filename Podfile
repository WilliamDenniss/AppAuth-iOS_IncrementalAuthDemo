target 'Example-iOS_ObjC' do
  platform :ios, '9.0'

  # AppAuth Pod
  # In production, just use `pod 'AppAuth'` without the path reference.
  pod 'AppAuth', :git => 'https://github.com/WilliamDenniss/AppAuth-iOS', :branch => 'draft-ietf-oauth-incremental-authz'
end


